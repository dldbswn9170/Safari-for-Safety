# 🌐 외부 네트워크에서 접속하기

컴퓨터를 서버로 사용해서 **다른 WiFi나 LTE**에서도 접속할 수 있게 하는 방법

---

## ✅ 현재 상황

- 같은 WiFi: ✅ 접속 가능 (http://203.234.62.145:3000)
- 다른 WiFi/LTE: ❌ 접속 불가 → **이걸 해결하려면 아래 방법 사용**

---

## 🚀 방법 1: ngrok 사용 (추천)

### 장점
- ✅ 5분만에 설정 완료
- ✅ 공유기 설정 필요 없음
- ✅ HTTPS 자동 제공 (보안)
- ✅ 무료로 사용 가능

### 단점
- ❌ URL이 매번 바뀜 (무료 버전)
- ❌ ngrok 실행 중에만 접속 가능

### 사용 방법

#### 1단계: 서버 시작
```
start-server.bat 더블클릭
```

#### 2단계: ngrok 실행
```
start-ngrok.bat 더블클릭
```

#### 3단계: URL 확인
ngrok이 실행되면 이런 화면이 나옵니다:
```
Forwarding: https://1a2b-3c4d-5e6f.ngrok-free.app -> http://localhost:5000
```

#### 4단계: URL 공유
위에 나온 URL을 다른 사람들에게 공유:
```
https://1a2b-3c4d-5e6f.ngrok-free.app
```

**이 주소로 전세계 어디서든 접속 가능!** 🎉

---

## 🏠 방법 2: 공유기 포트포워딩 (영구)

### 장점
- ✅ URL이 고정됨
- ✅ 컴퓨터만 켜놓으면 항상 접속 가능
- ✅ 추가 프로그램 필요 없음

### 단점
- ❌ 공유기 설정 필요 (10-20분)
- ❌ 공인 IP가 바뀔 수 있음 (DDNS 필요)
- ❌ 보안 설정 신경써야 함

### 사용 방법

#### 1단계: 공유기 관리 페이지 접속
브라우저에서 다음 중 하나 입력:
- `http://192.168.0.1`
- `http://192.168.1.1`
- `http://192.168.219.1` (KT)

#### 2단계: 로그인
- 공유기 관리자 아이디/비밀번호 입력
- (보통 admin/admin 또는 공유기 뒷면에 적혀있음)

#### 3단계: 포트포워딩 설정
**고급 설정** → **NAT/라우터 관리** → **포트포워딩** 메뉴 찾기

**Backend 포트포워딩 추가:**
| 항목 | 값 |
|------|-----|
| 서비스 이름 | Safari-Backend |
| 내부 IP | 203.234.62.145 |
| 외부 포트 | 5000 |
| 내부 포트 | 5000 |
| 프로토콜 | TCP |

**Frontend 포트포워딩 추가:**
| 항목 | 값 |
|------|-----|
| 서비스 이름 | Safari-Frontend |
| 내부 IP | 203.234.62.145 |
| 외부 포트 | 3000 |
| 내부 포트 | 3000 |
| 프로토콜 | TCP |

#### 4단계: 공인 IP 확인
터미널에서 실행:
```bash
curl ifconfig.me
```

나온 IP 주소가 공인 IP (예: 123.456.789.012)

#### 5단계: 외부에서 접속
```
http://[공인IP]:3000
```

예: `http://123.456.789.012:3000`

---

## 📊 비교표

| 항목 | ngrok | 포트포워딩 |
|------|-------|------------|
| 설정 난이도 | ⭐ 매우 쉬움 | ⭐⭐⭐ 중간 |
| 설정 시간 | 5분 | 20분 |
| URL 고정 | ❌ 매번 변경 | ✅ 고정 |
| 보안 | ✅ HTTPS | ⚠️ HTTP |
| 추가 프로그램 | ngrok 필요 | 필요 없음 |
| 추천 대상 | 테스트/임시 공유 | 장기 운영 |

---

## 🎯 추천 사용 방법

### 상황별 추천

1. **친구들에게 잠깐 보여줄 때**
   → ngrok 사용 (start-ngrok.bat)

2. **프로젝트 발표/데모**
   → ngrok 사용 (간편함)

3. **장기간 운영 (1주일 이상)**
   → 포트포워딩 설정

4. **회사/학교 네트워크**
   → ngrok 사용 (공유기 설정 권한 없을 수 있음)

---

## 🔒 보안 주의사항

### ngrok 사용 시
- ✅ HTTPS 자동 제공 (안전)
- ✅ ngrok URL은 공유한 사람만 알 수 있음
- ⚠️ URL을 공개하지 말 것

### 포트포워딩 사용 시
- ⚠️ 공인 IP가 노출됨
- ⚠️ 방화벽 설정 필수
- ⚠️ 강력한 비밀번호 사용
- 💡 HTTPS 적용 권장 (Let's Encrypt)

---

## 🛠️ 문제 해결

### ngrok 실행 안됨
```bash
# ngrok 재다운로드
# https://ngrok.com/download
```

### "Backend 서버가 실행되지 않았습니다"
```bash
# start-server.bat 먼저 실행
# 5-10초 대기 후 start-ngrok.bat 실행
```

### 포트포워딩 후에도 접속 안됨
1. ✅ 방화벽 규칙 확인 (setup-firewall.bat 실행)
2. ✅ 공인 IP 다시 확인 (curl ifconfig.me)
3. ✅ 공유기 설정 저장 & 재부팅
4. ✅ 컴퓨터 내부 IP 고정 (DHCP 예약)

---

## 📱 사용자에게 공유할 메시지

### ngrok 사용 시
```
🦁 Safari-for-Safety 서버가 열렸습니다!

🌐 접속 주소:
https://xxxx-xxxx-xxxx.ngrok-free.app

📱 휴대폰, PC 어디서든 접속 가능합니다!
⚠️ 서버가 켜져있을 때만 접속 가능
```

### 포트포워딩 사용 시
```
🦁 Safari-for-Safety 서버가 열렸습니다!

🌐 접속 주소:
http://[공인IP]:3000

📱 휴대폰, PC 어디서든 접속 가능합니다!
⚠️ 제 컴퓨터가 켜져있을 때만 접속 가능
```

---

## 🎯 다음 단계

### ngrok을 선택했다면:
1. `start-ngrok.bat` 실행
2. URL 복사
3. 공유!

### 포트포워딩을 선택했다면:
1. 공유기 설정
2. 공인 IP 확인
3. `http://[공인IP]:3000` 공유!

---

**어떤 방법을 선택하시나요?**
- 간단하게 테스트하고 싶다면 → **ngrok**
- 오래 운영하고 싶다면 → **포트포워딩**
